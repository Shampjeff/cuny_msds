---
title: "Project 1 Chess Table"
author: "Jeff Shamp"
date: "2/13/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r, include=FALSE}
library(tidyverse)
library(RCurl)

```

```{r}
# df<-read.csv('tournamentinfo.txt', header = TRUE)
# df<-df[df != strrep("-",89)]
# df<-df[3:130]

```

```{r}
# df<-str_extract_all(df, regex("\\b[^|[:blank:]]*([0-9]+([.][0-9]))*\\b", ignore_case=F, multiline=T))
# df<-lapply(df, function(x) x[!x %in% ""])
```


```{r}
data<- readLines("tournamentinfo.txt")
data<-str_extract_all(data, regex("\\b[^|-[:blank:]]*([0-9]+([.][0-9]))*\\b", ignore_case =FALSE, multiline = TRUE))
data<-data[5:130]
data<-unlist(lapply(data, function(x) x[!x %in% ""]))
data[700:1300]
```

```{r}
uscf_regex<-"^[0-9]{8}$"
uscf_id<-unlist(str_extract_all(data, uscf_regex))
uscf_id
```

```{r}
points_regex<-"[:digit:]\\.[:digit:]"
points<-unlist(str_extract_all(data, points_regex))
points
```

```{r}
state_regex<-"^[A-Z]{2}$"
states<-unlist(str_extract_all(data, state_regex))
states
```

```{r}
name_regex<-"[:alpha:]{3,}"
name<- unlist(str_extract_all(data, name_regex))
name
```


```{r}
score_regex<-"^[:digit:]{4}$"
score<-unlist(str_extract_all(data, score_regex))
score
```












```{r}
col_names<-c('index','first_name','last_name', 'score', 'game_1_out', 'game_1_op','game_2_out','game_2_op', 'game_3_out', 'game_3_out', 'game_4_out', 'game_4_op', 'game_5_out', 'game_5_op', 'game_6_out', 'game_6_op', 'state', 'nonsense', 'nonsense_2', 'prev_elo', 'new_elo', 'nonsense_3', 'color_1','color_2','color_3','color_4', 'color_5','color_6')
```























